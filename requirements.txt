# KTX Seat Monitor Step 5B
streamlit==1.60.0
pandas>=2.2,<3
requests>=2.32,<3
# 2.18.1+ uses the internal package name supabase_auth,
# which conflicts with the current local module supabase_auth.py.
supabase==2.17.0
